# فاز اول: ارزیابی مدل‌های مقاله روی Nothing Phone (2a)

**وضعیت:** انجام شد (۲۰۲۶-۰۹-۲۴). گزارش کامل: [`reports/phase1-nothing-phone-2a.md`](reports/phase1-nothing-phone-2a.md).  
**هدف:** اجرای مدل‌های کم‌نور موجود روی یک گوشی واقعی و ثبت کیفیت، زمان و منابع مصرفی برای مطالعه‌ی موردی مقاله‌ی مروری.

## نتیجه‌ی خلاصه

| مدل | بارگذاری (ms) | میانه‌ی model (ms) | p95 model (ms) | PSS peak (MB) | PSNR | SSIM | LPIPS |
|---|---:|---:|---:|---:|---:|---:|---:|
| zero-dce | 240.5 | 2324.9 | 2371.5 | 613.5 | 14.80 | 0.561 | 0.335 |
| sci-medium | 122.7 | 70.4 | 85.3 | 218.7 | 14.78 | 0.525 | 0.339 |

هر دو اجرا `success=true` و schema-mvalid؛ ۱۵/۱۵ خروجی ذخیره شد. فقط CPU (ONNX Runtime 1.19.2). اعداد بالا برای ۶۰۰×۴۰۰ روی eval15 است؛ جزئیات و محدودیت‌ها در گزارش فاز.

## دامنه

- یک گوشی: Nothing Phone (2a). نسخه‌ی Android، build و مشخصات SoC پس از اتصال ثبت می‌شوند.
- عکس ثابت با وضوح اصلی؛ عکس‌های حدود ۱۲ مگاپیکسل برای آزمون فشار حافظه و زمان اجرا.
- اجرای CPU و هر شتاب‌دهنده‌ی پشتیبانی‌شده در مسیرهای جداگانه.
- مدل‌های آغازین: Zero-DCE و SCI medium، مشروط به موفقیت در آزمون export و برابری خروجی.
- اپ اندروید اجرای مدل و زمان‌سنجی را داخل گوشی انجام می‌دهد. Python/ADB نصب، انتقال دسته‌ای، آغاز اجرا و دریافت گزارش را هماهنگ می‌کند.
- نتیجه‌ی یک گوشی فقط مطالعه‌ی موردی است؛ رتبه‌بندی عمومی سه‌رده‌ای متعلق به فاز دوم است.

## مراحل

۱. **آماده‌سازی محیط:** نصب JDK و Android Studio، سپس Android SDK Platform-Tools. در بررسی فعلی، Java، Gradle و ADB در PATH نبودند؛ winget در دسترس است.
۲. **اتصال گوشی:** فعال‌کردن USB debugging، تأیید اتصال، ثبت Android release/API، build fingerprint و مشخصات SoC.
۳. **سازگاری مدل:** آزمون Zero-DCE و SCI با ONNX Runtime و LiteRT؛ بررسی export، اختلاف خروجی با PyTorch و پشتیبانی CPU/GPU/NPU. تنها یک runtime پس از این آزمون وارد اپ می‌شود.
۴. **ساخت runner:** اپ Kotlin برای انتخاب مدل/تصویر، اجرای batch=1، سنجش درون‌فرایندی با ساعت یکنواخت گوشی، ذخیره‌ی خروجی و JSON. ADB زمان هر تصویر را اندازه نمی‌گیرد.
۵. **کنترل و تحلیل میزبان:** Python/ADB برای اجرای خودکار؛ محاسبه‌ی PSNR، SSIM و LPIPS پس از اجرا و خارج از بازه‌ی زمان‌گیری.
۶. **آزمون و گزارش:** اجرای مجموعه‌ی جفت‌شده‌ی `04_datasets/paired/eval15` در وضوح اصلی و مجموعه‌ی مستقلِ وضوح‌بالا برای فشار اجرا. اگر تصویر وضوح‌بالا مرجع جفت‌شده‌ی مجاز نداشته باشد، فقط زمان، حافظه و پایداری گزارش می‌شوند.

## خروجی‌های فاز

- APK اندروید برای اجرای دستی و runner مشترک با کنترل‌گر ADB.
- manifest مدل و JSON هر اجرا با hash مدل، دستگاه، build، runtime، backend، زمان بارگذاری، زمان شبکه، زمان سرتاسری، حافظه، وضعیت حرارتی و وضعیت موفقیت/خطا.
- CSV و گزارش مطالعه‌ی موردی شامل میانه و صدک ۹۵ تأخیر، کیفیت روی جفت‌های معتبر و رخدادهای OOM یا backend پشتیبانی‌نشده.
- انرژی به‌عنوان ژول بر تصویر گزارش نمی‌شود مگر آن‌که با توان‌سنج بیرونی اعتبارسنجی شود.

## معیار پایان

- اپ روی Nothing Phone (2a) نصب و اجرا شود.
- دست‌کم دو مدل اجرا شوند یا ناسازگاری هر مدل مستند شود.
- گزارش از فایل‌های JSON و تصاویر ذخیره‌شده دوباره تولید شود.
- CPU و backendهای در دسترس جدا گزارش شوند و نتیجه به‌عنوان مطالعه‌ی یک‌دستگاهی برچسب بخورد.

## اسناد مرتبط

- [طرح فنی](design.md)
- [برنامه‌ی اجرایی جزئی](docs/superpowers/plans/2026-09-24-mobile-llie-phase1.md)
