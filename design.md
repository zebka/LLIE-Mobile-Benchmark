# طرح فنی: بنچمارک استقرار موبایلی بهبود کم‌نور

**وضعیت:** طرح فنی تأیید و فاز اول اجرا شد (۲۰۲۶-۰۹-۲۴)؛ نتایج در `reports/phase1-nothing-phone-2a.md`.  
**دامنه‌ی شروع:** ارزیابی مدل‌های مقاله‌ی مروری روی یک گوشی  
**هدف نهایی:** انتشار بنچمارک باز و بازتولیدپذیر روی سه رده‌ی گوشی مرجع

## ۱. مسئله و هدف

گزارش‌های رایج LLIE کیفیت خروجی را می‌سنجند، اما از روی تعداد پارامتر یا حجم فایل نمی‌توان زمان اجرا، RAM، افت حرارتی و انرژی روی گوشی را نتیجه گرفت. این پروژه یک اپ اندروید و کنترل‌گر میزبان می‌سازد تا مدل روی گوشی واقعی اجرا شود و هزینه‌ی انتها‌به‌انتها ثبت شود.

فاز اول مطالعه‌ی موردی مقاله‌ی مروری است؛ یک گوشی، عکس ثابت و مجموعه‌ای محدود از مدل‌های قابل‌دسترسی. این فاز ادعای رتبه‌بندی عمومی یا تعمیم به تمام گوشی‌ها نخواهد داشت. فاز دوم، با سه رده‌ی سخت‌افزاری ثابت، پروتکل و ابزارها را به بنچمارک عمومی تبدیل می‌کند.

## ۲. دامنه

### فاز اول: مطالعه‌ی موردی

- دستگاه: Nothing Phone (2a). نسخه‌ی دقیق Android، build، تراشه و runtime هنگام اتصال از خود دستگاه ثبت می‌شود.
- ورودی: عکس ثابت؛ اجرای وضوح کامل اولویت دارد و هدف آزمون تنش ۱۲ مگاپیکسل است.
- backendها: CPU و شتاب‌دهنده‌ی GPU/NPU جداگانه، فقط اگر runtime و دستگاه از آن پشتیبانی کنند.
- مدل‌های آغازین: از پیاده‌سازی‌ها و وزن‌های موجود در آرشیو LLIE انتخاب می‌شوند؛ Zero-DCE و SCI گزینه‌های نخست برای آزمون سازگاری‌اند. فهرست قطعی پس از بررسی ورودی/خروجی و امکان export ثبت می‌شود.
- داده: مجموعه‌ی جفت‌شده‌ی موجود برای سنجش کیفیت در وضوح اصلی خودش استفاده می‌شود. آزمون ۱۲ مگاپیکسلی، در صورت نبودِ جفت مرجع مجاز و هم‌تراز، فقط آزمون هزینه و پایداری است؛ تصویر بزرگ‌نمایی‌شده جایگزین داده‌ی واقعی ۱۲ مگاپیکسلی نمی‌شود.
- خروجی فاز اول: تصاویر بهبودیافته، داده‌ی خام زمان و حافظه، اطلاعات دستگاه، و گزارش کیفیت فقط برای نمونه‌هایی که مرجع معتبر دارند.

### فاز دوم: بنچمارک عمومی

- سه دستگاه مرجع از رده‌های اقتصادی، میان‌رده و پرچم‌دار؛ هر دستگاه و backend رتبه‌بندی جداگانه دارد.
- شرکت‌کنندگان artifact استاندارد به‌همراه manifest تحویل می‌دهند؛ کد دلخواه هر تیم در مسیر اصلی leaderboard اجرا نمی‌شود.
- امتیاز کلی از نوع «کیفیت در بودجه» است: بودجه‌های اجرایی پیش از ارزیابی تعیین می‌شوند؛ مدل‌های واجدشرایط بر پایه‌ی کیفیت امتیاز می‌گیرند.
- نتایج خام و جبهه‌های پارتو کنار امتیاز کلی منتشر می‌شوند. انرژی رسمی فقط با ابزار توان‌سنجی معتبر روی دستگاه مرجع گزارش می‌شود.

## ۳. معماری نرم‌افزار

### اپ اندروید: اجراکننده‌ی بنچمارک

- اپ بومی اندروید، مدل، backend و مجموعه‌ی تصاویر را انتخاب می‌کند و امکان اجرای دستی بدون رایانه را هم دارد.
- هر اجرا batch size برابر ۱ دارد. زمان‌سنجی داخل فرایند اپ و با ساعت یکنواخت دستگاه انجام می‌شود؛ هیچ فرمان ADB در حلقه‌ی اندازه‌گیری هر تصویر اجرا نمی‌شود.
- دو زمان ثبت می‌شود: زمان اجرای شبکه و زمان انتها‌به‌انتها شامل پیش‌پردازش، tiling مجاز، اجرای مدل، ادغام وصله‌ها و پس‌پردازش.
- اپ artifact، metadata دستگاه، خروجی‌ها و رخدادهای خطا را در گزارش JSON ذخیره می‌کند.
- manifest مدل ورودی و خروجی، ترتیب کانال‌ها، رنگ‌فضا، دامنه‌ی مقدارها، دقت وزن‌ها و روش پیش‌/پس‌پردازش را مشخص می‌کند.

### کنترل‌گر میزبان: Python و ADB

- ADB برای نصب اپ، ثبت مشخصات دستگاه، انتقال مدل و داده، آغاز آزمون و دریافت خروجی‌ها به‌کار می‌رود.
- اسکریپت Python گزارش‌های دستگاه را اعتبارسنجی می‌کند، سنجه‌های مرجع‌دار تصویر را خارج از بازه‌ی زمان‌گیری محاسبه می‌کند و CSV/گزارش تجمیعی می‌سازد.
- اجرای دستی از داخل اپ و اجرای خودکار با ADB از یک runner مشترک استفاده می‌کنند تا تفاوت مسیر اجرا حداقل بماند.

### runtime و قالب مدل

قالب نهایی هنوز قفل نمی‌شود. یک آزمون سازگاری روی مدل‌های نماینده، export از پیاده‌سازی‌های موجود، پشتیبانی اپراتورها و اجرای CPU/GPU/NPU را میان ONNX Runtime و LiteRT مقایسه می‌کند. پس از انتخاب، نسخه‌ی runtime و تنظیمات delegate برای هر track پین می‌شوند. اگر یک backend اپراتوری را پشتیبانی نکند، آن اجرا «پشتیبانی‌نشده» ثبت می‌شود؛ نتیجه‌ی CPU جدا باقی می‌ماند.

## ۴. پروتکل سنجش

۱. اپ مشخصات گوشی، نسخه‌ی سیستم‌عامل، runtime، backend، دقت عددی و hash مدل را ثبت می‌کند.  
۲. مدل بارگذاری و مرحله‌ی warm-up اجرا می‌شود؛ زمان بارگذاری جدا از زمان استنتاج گزارش می‌شود.  
۳. تصاویر با ترتیب ثابت و batch size برابر ۱ پردازش می‌شوند. آزمون چند بار تکرار می‌شود تا میانه و صدک ۹۵ تأخیر محاسبه شوند.  
۴. برای وضوح کامل، زمان سرتاسری و بیشینه‌ی RAM شامل مدیریت وصله‌ها ثبت می‌شوند؛ OOM یا ناتوانی در تکمیل وضوح هدف شکست معتبر آزمون است، نه نمونه‌ای که بی‌صدا حذف شود.  
۵. اجرای پیوسته روی همان مجموعه، افت سرعت و وضعیت حرارتی را ثبت می‌کند. تعداد تکرار و زمان خنک‌سازی در نسخه‌ی نهایی runner ثابت می‌شوند.  
۶. معیارهای کیفیت روی خروجی ذخیره‌شده و خارج از بازه‌ی زمان‌گیری محاسبه می‌شوند. جفت‌های فاقد تراز معتبر وارد سنجه‌های مرجع‌دار نمی‌شوند.

ADB فقط فرمان‌های دسته‌ای می‌دهد؛ زمان هر تصویر از داخل اپ به‌دست می‌آید. برای فاز اول، قرائت نرم‌افزاری باتری به‌عنوان اندازه‌گیری رسمی ژول گزارش نمی‌شود.

## ۵. سنجه‌ها و گزارش

- تأخیر: میانه و صدک ۹۵ زمان شبکه و زمان سرتاسری، جدا از زمان بارگذاری.
- منابع: اندازه‌ی واقعی artifact، تعداد پارامتر، دقت وزن‌ها و بیشینه‌ی RAM.
- دوام: افت تأخیر در اجرای پیوسته و رخدادهای حرارتی یا OOM.
- کیفیت جفت‌شده: PSNR/SSIM و LPIPS/DISTS روی داده‌ی تراز و دارای مرجع.
- کیفیت بدون مرجع و ارزیابی انسانی: گزارش جداگانه؛ در نسخه‌ی اول به‌تنهایی تعیین‌کننده‌ی امتیاز اصلی نیستند.
- انرژی: در فاز عمومی و فقط پس از اعتبارسنجی با توان‌سنج خارجی، برحسب ژول بر تصویر.
- رتبه‌بندی فاز عمومی: جداگانه برای دستگاه و backend؛ امتیاز کلی کیفیت‌در‌بودجه به همراه داده‌های خام و پارتو.

## ۶. معیار موفقیت فاز اول

- اپ روی Nothing Phone (2a) نصب و اجرا شود.
- دست‌کم دو مدل منتخب با ورودی/خروجی مستند اجرا شوند یا دلیل دقیق ناسازگاری آن‌ها ثبت شود.
- اجرای CPU و هر backend پشتیبانی‌شده جدا ثبت شود.
- برای هر تصویر، خروجی و metadata کافی برای بازتولید نتیجه ذخیره شود.
- گزارش زمان، حافظه، اندازه‌ی مدل و کیفیت روی داده‌ی معتبر از اجرای مجدد قابل تولید باشد.

## ۷. وضعیت ابزار توسعه و تصمیم‌های مرحله‌ی بعد

در رایانه‌ی فعلی `java`، `gradle` و `adb` در PATH شناخته نشدند؛ `winget` موجود است. پیش از ساخت APK باید JDK، Android SDK/platform-tools و ابزار build نصب و نسخه‌هایشان ثبت شود. اتصال USB و مجوز USB debugging روی گوشی هنوز تأیید نشده است.

گام اجرایی بعدی پس از بازبینی این طرح: آماده‌سازی ابزار توسعه و اتصال Nothing Phone (2a)، ثبت نسخه‌ی Android، سپس آزمون سازگاری Zero-DCE و SCI با ONNX Runtime و LiteRT پیش از تثبیت runtime و قرارداد مدل.
